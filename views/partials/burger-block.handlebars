<li>
    {{dataValues.burger_name}}

    <button {{#if dataValues.devoured}} class="delete" {{else}} class="devour" {{/if}} data-id="{{dataValues.id}}" data-eaten="{{dataValues.devoured}}">
        {{#if dataValues.devoured}}Delete{{else}}Devour{{/if}}
    </button>
    <br><br>
</li>